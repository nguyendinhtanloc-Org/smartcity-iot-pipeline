services:
  telegraf:
    image: telegraf:1.30
    container_name: telegraf-multi-mqtt
    volumes:
      - ./telegraf.conf:/etc/telegraf/telegraf.conf:ro
      - ./data:/data
    restart: unless-stopped
